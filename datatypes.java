import java.util.*;

public class datatypes {
    public static void main(String[] args) {
        List<String> arr = new ArrayList<>();
        int[] i = { 1, 2, 3 };
        HashMap<String, Integer> mp = new HashMap<>(); // u are using specific HashMap method only
        Map<String, Integer> m = new HashMap<>(); // this is a interface
    }
}
